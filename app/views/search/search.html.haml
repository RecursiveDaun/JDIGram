=form_for search_path, method: :get do |f|
  %p
    =f.label "Search for"
    = text_field_tag :q, params[:q]
    = submit_tag "search", name: nil

- if @users.present?
  - @users.each do |user|
    %label= user.nickname