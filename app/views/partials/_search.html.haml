=form_for search_path, method: :get do |f|
  .col-lg-12.text-center
    .input-group
      = text_field_tag :q, params[:q], placeholder: 'Enter username or email', class: 'form-control'
      .input-group-append
        = submit_tag "find", name: nil, class: 'btn btn-secondary'